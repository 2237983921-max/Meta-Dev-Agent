services:
  - type: web
    name: memeradar-live
    runtime: python
    plan: free
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --app-dir . --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
