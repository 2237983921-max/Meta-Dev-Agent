/* /home/user/meme-tracker/css/styles.css */

/* ===== 全局样式 ===== */
* { margin: 0; padding: 0; box-sizing: border-box; }

:root {
  --bg-primary: #0a0e1a;
  --bg-secondary: #111827;
  --bg-card: #1a2035;
  --bg-card-hover: #1f2847;
  --border: rgba(255,255,255,0.06);
  --border-light: rgba(255,255,255,0.1);
  --text-primary: #f1f5f9;
  --text-secondary: #94a3b8;
  --text-muted: #64748b;
  --accent-blue: #00d4ff;
  --accent-green: #00ff88;
  --accent-red: #ff2d57;
  --accent-purple: #7b2ff7;
  --accent-yellow: #ffe066;
  --accent-orange: #ff9f43;
  --gradient-primary: linear-gradient(135deg, #00d4ff, #7b2ff7);
  --gradient-hot: linear-gradient(135deg, #ff2d57, #ff9f43);
  --shadow-lg: 0 8px 32px rgba(0,0,0,0.3);
  --radius: 12px;
  --radius-sm: 8px;
  --sidebar-width: 220px;
  --topbar-height: 60px;
}

html { font-size: 14px; }

body {
  background: var(--bg-primary);
  color: var(--text-primary);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Noto Sans SC', sans-serif;
  overflow-x: hidden;
}

/* 滚动条 */
::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.1); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: rgba(255,255,255,0.2); }

/* ===== 顶部导航 ===== */
.topbar {
  position: fixed;
  top: 0; left: 0; right: 0;
  height: var(--topbar-height);
  background: rgba(10, 14, 26, 0.85);
  backdrop-filter: blur(20px);
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 24px;
  z-index: 100;
}

.topbar-left, .topbar-right { display: flex; align-items: center; gap: 16px; }
.topbar-center { flex: 1; max-width: 500px; margin: 0 32px; }

.logo { display: flex; align-items: center; gap: 8px; font-size: 18px; font-weight: 700; }
.logo-icon { font-size: 24px; }
.logo-text { letter-spacing: -0.5px; }
.accent { background: var(--gradient-primary); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }

.logo-badge {
  font-size: 9px;
  padding: 2px 6px;
  background: var(--accent-red);
  border-radius: 4px;
  font-weight: 700;
  animation: pulse-badge 2s infinite;
  letter-spacing: 1px;
}

@keyframes pulse-badge {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

.search-box {
  display: flex;
  align-items: center;
  gap: 8px;
  background: var(--bg-secondary);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  padding: 8px 12px;
  transition: border-color 0.3s;
}

.search-box:focus-within { border-color: var(--accent-blue); }
.search-box input {
  flex: 1;
  background: none;
  border: none;
  outline: none;
  color: var(--text-primary);
  font-size: 13px;
}
.search-box input::placeholder { color: var(--text-muted); }
.search-icon { color: var(--text-muted); flex-shrink: 0; }
.search-box kbd {
  font-size: 10px;
  padding: 2px 6px;
  background: rgba(255,255,255,0.08);
  border-radius: 4px;
  color: var(--text-muted);
  font-family: inherit;
  border: 1px solid var(--border);
}
.search-box.compact { max-width: 300px; }

.icon-btn {
  position: relative;
  background: rgba(255,255,255,0.05);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  color: var(--text-secondary);
  width: 36px; height: 36px;
  display: flex; align-items: center; justify-content: center;
  cursor: pointer;
  transition: all 0.2s;
}
.icon-btn:hover { background: rgba(255,255,255,0.1); color: var(--text-primary); }

.badge {
  position: absolute;
  top: -4px; right: -4px;
  background: var(--accent-red);
  color: #fff;
  font-size: 10px;
  min-width: 16px; height: 16px;
  border-radius: 8px;
  display: flex; align-items: center; justify-content: center;
  font-weight: 700;
}

.connection-status {
  display: flex; align-items: center; gap: 6px;
  font-size: 12px; color: var(--text-muted);
}
.status-dot {
  width: 8px; height: 8px; border-radius: 50%;
  background: var(--text-muted);
}
.status-dot.live {
  background: var(--accent-green);
  box-shadow: 0 0 8px var(--accent-green);
  animation: pulse-dot 2s infinite;
}
@keyframes pulse-dot {
  0%, 100% { box-shadow: 0 0 4px var(--accent-green); }
  50% { box-shadow: 0 0 12px var(--accent-green); }
}

/* ===== 布局 ===== */
.layout {
  display: flex;
  padding-top: var(--topbar-height);
  min-height: 100vh;
}

/* ===== 侧边栏 ===== */
.sidebar {
  position: fixed;
  top: var(--topbar-height);
  left: 0;
  bottom: 0;
  width: var(--sidebar-width);
  background: var(--bg-secondary);
  border-right: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  z-index: 50;
  overflow-y: auto;
}

.nav-menu {
  list-style: none;
  padding: 12px 8px;
  flex: 1;
}

.nav-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 10px 14px;
  border-radius: var(--radius-sm);
  color: var(--text-secondary);
  cursor: pointer;
  transition: all 0.2s;
  margin-bottom: 2px;
  font-size: 13px;
  position: relative;
}

.nav-item:hover { background: rgba(255,255,255,0.05); color: var(--text-primary); }
.nav-item.active {
  background: rgba(0, 212, 255, 0.1);
  color: var(--accent-blue);
}
.nav-item.active::before {
  content: '';
  position: absolute;
  left: 0; top: 50%;
  transform: translateY(-50%);
  width: 3px; height: 20px;
  background: var(--accent-blue);
  border-radius: 0 3px 3px 0;
}

.nav-badge {
  margin-left: auto;
  background: var(--accent-red);
  color: #fff;
  font-size: 10px;
  min-width: 18px; height: 18px;
  border-radius: 9px;
  display: flex; align-items: center; justify-content: center;
  font-weight: 700;
}

.sidebar-footer {
  padding: 16px;
  border-top: 1px solid var(--border);
}

.stats-mini { display: flex; flex-direction: column; gap: 8px; }
.stat-mini-item { display: flex; flex-direction: column; }
.stat-mini-val { font-size: 16px; font-weight: 700; color: var(--accent-blue); }
.stat-mini-label { font-size: 11px; color: var(--text-muted); }

/* ===== 主内容区 ===== */
.main-content {
  margin-left: var(--sidebar-width);
  flex: 1;
  padding: 24px;
  overflow-y: auto;
}

.page { display: none; }
.page.active { display: block; }

.page-header { margin-bottom: 24px; }
.page-header h1 { font-size: 24px; margin-bottom: 4px; }
.page-header p { color: var(--text-muted); font-size: 14px; }

/* ===== 统计卡片 ===== */
.stats-row {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 16px;
  margin-bottom: 24px;
}

.stat-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 20px;
  display: flex;
  align-items: center;
  gap: 14px;
  transition: all 0.3s;
}
.stat-card:hover { border-color: var(--border-light); transform: translateY(-2px); box-shadow: var(--shadow-lg); }

.stat-card-icon {
  width: 48px; height: 48px;
  border-radius: 12px;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
}

.stat-card-data { flex: 1; }
.stat-card-value { font-size: 24px; font-weight: 700; }
.stat-card-label { font-size: 12px; color: var(--text-muted); margin-top: 2px; }

.stat-card-trend {
  font-size: 11px;
  padding: 4px 8px;
  border-radius: 6px;
  font-weight: 600;
}
.stat-card-trend.up { background: rgba(0,255,136,0.1); color: var(--accent-green); }
.stat-card-trend.hot { background: rgba(255,45,87,0.1); color: var(--accent-red); }

/* ===== 面板 ===== */
.dashboard-grid {
  display: grid;
  grid-template-columns: 1.2fr 1fr;
  gap: 16px;
}

.panel {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  overflow: hidden;
}

.panel-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px 20px;
  border-bottom: 1px solid var(--border);
}
.panel-header h2 { font-size: 14px; font-weight: 600; }

.panel-actions { display: flex; align-items: center; gap: 8px; }
.panel-actions select {
  background: var(--bg-secondary);
  border: 1px solid var(--border);
  color: var(--text-secondary);
  padding: 4px 8px;
  border-radius: 6px;
  font-size: 12px;
  cursor: pointer;
  outline: none;
}

.btn-sm {
  background: rgba(255,255,255,0.05);
  border: 1px solid var(--border);
  color: var(--text-secondary);
  width: 28px; height: 28px;
  border-radius: 6px;
  display: flex; align-items: center; justify-content: center;
  cursor: pointer;
  transition: all 0.2s;
  font-size: 12px;
}
.btn-sm:hover { background: rgba(255,255,255,0.1); }

.panel-tabs { display: flex; gap: 4px; }
.tab {
  background: none;
  border: 1px solid var(--border);
  color: var(--text-muted);
  padding: 4px 10px;
  border-radius: 6px;
  font-size: 11px;
  cursor: pointer;
  transition: all 0.2s;
  font-weight: 600;
}
.tab.active, .tab:hover { background: rgba(0,212,255,0.1); color: var(--accent-blue); border-color: rgba(0,212,255,0.3); }

/* ===== 推文流 ===== */
.feed-list {
  max-height: 500px;
  overflow-y: auto;
}
.feed-list.full-feed { max-height: calc(100vh - 280px); }

.feed-item {
  display: flex;
  gap: 12px;
  padding: 14px 20px;
  border-bottom: 1px solid var(--border);
  transition: background 0.2s;
  animation: slideIn 0.4s ease-out;
  cursor: pointer;
}
.feed-item:hover { background: rgba(255,255,255,0.02); }
.feed-item.highlight { background: rgba(0, 212, 255, 0.05); border-left: 3px solid var(--accent-blue); }
.feed-item.alert-new { animation: slideIn 0.4s ease-out, glowAlert 2s ease-out; }

@keyframes slideIn {
  from { opacity: 0; transform: translateY(-10px); }
  to { opacity: 1; transform: translateY(0); }
}
@keyframes glowAlert {
  0% { background: rgba(0, 212, 255, 0.15); }
  100% { background: transparent; }
}

.feed-avatar {
  width: 40px; height: 40px;
  border-radius: 50%;
  background: var(--bg-secondary);
  display: flex; align-items: center; justify-content: center;
  font-size: 18px;
  flex-shrink: 0;
  border: 2px solid var(--border);
}
.feed-avatar.kol { border-color: var(--accent-yellow); }

.feed-content { flex: 1; min-width: 0; }
.feed-user { display: flex; align-items: center; gap: 6px; margin-bottom: 4px; }
.feed-username { font-weight: 600; font-size: 13px; }
.feed-handle { color: var(--text-muted); font-size: 12px; }
.feed-time { color: var(--text-muted); font-size: 11px; margin-left: auto; }
.feed-text { font-size: 13px; line-height: 1.5; color: var(--text-secondary); word-break: break-word; }
.feed-text .token-mention {
  color: var(--accent-blue);
  font-weight: 600;
  cursor: pointer;
  background: rgba(0,212,255,0.1);
  padding: 1px 4px;
  border-radius: 3px;
}
.feed-text .hashtag { color: var(--accent-purple); }

.feed-meta { display: flex; align-items: center; gap: 12px; margin-top: 8px; }
.feed-stat { font-size: 11px; color: var(--text-muted); display: flex; align-items: center; gap: 4px; }

.feed-tags { display: flex; gap: 4px; margin-top: 6px; flex-wrap: wrap; }
.feed-tag {
  font-size: 10px;
  padding: 2px 8px;
  border-radius: 4px;
  font-weight: 600;
}
.feed-tag.new-token { background: rgba(0,255,136,0.1); color: var(--accent-green); }
.feed-tag.hot { background: rgba(255,45,87,0.1); color: var(--accent-red); }
.feed-tag.kol { background: rgba(255,224,102,0.1); color: var(--accent-yellow); }
.feed-tag.pump { background: rgba(123,47,247,0.1); color: var(--accent-purple); }

/* ===== 代币表格 ===== */
.token-table { padding: 0; }
.token-table-header, .token-row {
  display: grid;
  grid-template-columns: 40px 1.5fr 0.8fr 0.8fr 1fr 60px;
  align-items: center;
  padding: 10px 20px;
  gap: 8px;
}
.token-table-header {
  font-size: 11px;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  border-bottom: 1px solid var(--border);
}

.token-table-body { max-height: 350px; overflow-y: auto; }

.token-row {
  border-bottom: 1px solid var(--border);
  transition: background 0.2s;
  cursor: pointer;
  font-size: 13px;
}
.token-row:hover { background: rgba(255,255,255,0.03); }

.col-rank { color: var(--text-muted); font-weight: 700; font-size: 12px; }
.col-name { display: flex; align-items: center; gap: 8px; }
.token-icon-small {
  width: 28px; height: 28px;
  border-radius: 50%;
  background: var(--bg-secondary);
  display: flex; align-items: center; justify-content: center;
  font-size: 14px;
}
.token-info-col .token-ticker { font-weight: 700; }
.token-info-col .token-chain-tag { font-size: 10px; color: var(--text-muted); }

.col-sentiment .sentiment-bar {
  width: 60px; height: 6px;
  background: rgba(255,255,255,0.1);
  border-radius: 3px;
  overflow: hidden;
}
.sentiment-bar-fill {
  height: 100%;
  border-radius: 3px;
  transition: width 0.5s;
}
.sentiment-bar-fill.positive { background: var(--accent-green); }
.sentiment-bar-fill.negative { background: var(--accent-red); }
.sentiment-bar-fill.neutral { background: var(--accent-yellow); }

.col-chart canvas { width: 80px; height: 30px; }

.col-action .action-btn {
  background: rgba(0,212,255,0.1);
  border: none;
  color: var(--accent-blue);
  padding: 4px 10px;
  border-radius: 6px;
  font-size: 11px;
  cursor: pointer;
  font-weight: 600;
  transition: all 0.2s;
}
.col-action .action-btn:hover { background: rgba(0,212,255,0.2); }

/* ===== 提醒列表 ===== */
.alerts-list { max-height: 300px; overflow-y: auto; }
.alerts-list.full-alerts { max-height: calc(100vh - 400px); }

.alert-item {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  padding: 12px 20px;
  border-bottom: 1px solid var(--border);
  animation: slideIn 0.3s ease-out;
}
.alert-item.unread { background: rgba(0,212,255,0.03); }

.alert-icon {
  width: 32px; height: 32px;
  border-radius: 8px;
  display: flex; align-items: center; justify-content: center;
  font-size: 16px;
  flex-shrink: 0;
}
.alert-icon.new-token { background: rgba(0,255,136,0.1); }
.alert-icon.kol-mention { background: rgba(255,224,102,0.1); }
.alert-icon.spike { background: rgba(255,45,87,0.1); }
.alert-icon.whale { background: rgba(123,47,247,0.1); }

.alert-content { flex: 1; }
.alert-title { font-size: 13px; font-weight: 600; margin-bottom: 2px; }
.alert-desc { font-size: 12px; color: var(--text-muted); }
.alert-time { font-size: 11px; color: var(--text-muted); margin-top: 4px; }

/* ===== 热力图 / 话题云 ===== */
.heatmap-container {
  padding: 20px;
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  align-items: center;
  justify-content: center;
  min-height: 200px;
}

.heatmap-item {
  padding: 6px 14px;
  border-radius: 20px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s;
  white-space: nowrap;
}
.heatmap-item:hover { transform: scale(1.1); }

/* ===== 趋势代币网格 ===== */
.trending-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 16px;
}

.trending-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 20px;
  cursor: pointer;
  transition: all 0.3s;
}
.trending-card:hover { border-color: var(--border-light); transform: translateY(-2px); box-shadow: var(--shadow-lg); }

.trending-card-header { display: flex; align-items: center; gap: 12px; margin-bottom: 16px; }
.trending-card-icon {
  width: 44px; height: 44px;
  border-radius: 50%;
  background: var(--bg-secondary);
  display: flex; align-items: center; justify-content: center;
  font-size: 22px;
}
.trending-card-name { font-weight: 700; font-size: 16px; }
.trending-card-ticker { color: var(--text-muted); font-size: 13px; }

.trending-card-stats { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-bottom: 16px; }
.trending-stat { display: flex; flex-direction: column; }
.trending-stat-label { font-size: 11px; color: var(--text-muted); }
.trending-stat-value { font-size: 16px; font-weight: 700; }
.trending-stat-value.up { color: var(--accent-green); }
.trending-stat-value.down { color: var(--accent-red); }

.trending-card-chart { height: 60px; }

/* ===== KOL 网格 ===== */
.kol-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 16px;
}

.kol-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 20px;
  transition: all 0.3s;
}
.kol-card:hover { border-color: var(--border-light); }

.kol-header { display: flex; align-items: center; gap: 12px; margin-bottom: 12px; }
.kol-avatar {
  width: 48px; height: 48px;
  border-radius: 50%;
  background: var(--bg-secondary);
  display: flex; align-items: center; justify-content: center;
  font-size: 24px;
  border: 2px solid var(--accent-yellow);
}
.kol-name { font-weight: 700; }
.kol-handle { color: var(--text-muted); font-size: 13px; }
.kol-followers { font-size: 12px; color: var(--text-muted); }

.kol-recent { margin-top: 12px; }
.kol-recent h4 { font-size: 12px; color: var(--text-muted); margin-bottom: 8px; }
.kol-tweet {
  font-size: 13px;
  color: var(--text-secondary);
  line-height: 1.5;
  padding: 10px;
  background: var(--bg-secondary);
  border-radius: var(--radius-sm);
}
.kol-tweet .token-mention { color: var(--accent-blue); font-weight: 600; }

/* ===== 设置页面 ===== */
.settings-container { max-width: 600px; }
.settings-group {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 20px;
  margin-bottom: 16px;
}
.settings-group h3 { font-size: 15px; margin-bottom: 16px; }
.setting-row { margin-bottom: 14px; }
.setting-row label { display: flex; align-items: center; gap: 8px; font-size: 13px; color: var(--text-secondary); cursor: pointer; }
.setting-input {
  width: 100%;
  background: var(--bg-secondary);
  border: 1px solid var(--border);
  color: var(--text-primary);
  padding: 10px 14px;
  border-radius: var(--radius-sm);
  font-size: 13px;
  margin-top: 6px;
  outline: none;
  font-family: inherit;
  transition: border-color 0.3s;
}
.setting-input:focus { border-color: var(--accent-blue); }
textarea.setting-input { resize: vertical; }
.inline-input {
  width: 60px;
  background: var(--bg-secondary);
  border: 1px solid var(--border);
  color: var(--text-primary);
  padding: 4px 8px;
  border-radius: 4px;
  text-align: center;
  outline: none;
}

input[type="checkbox"] {
  -webkit-appearance: none;
  appearance: none;
  width: 18px; height: 18px;
  border: 2px solid var(--border-light);
  border-radius: 4px;
  cursor: pointer;
  position: relative;
  flex-shrink: 0;
}
input[type="checkbox"]:checked {
  background: var(--accent-blue);
  border-color: var(--accent-blue);
}
input[type="checkbox"]:checked::after {
  content: '✓';
  position: absolute;
  top: 50%; left: 50%;
  transform: translate(-50%, -50%);
  color: #fff;
  font-size: 12px;
  font-weight: 700;
}

/* ===== 按钮 ===== */
.btn-primary {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 10px 24px;
  background: var(--gradient-primary);
  color: #fff;
  border: none;
  border-radius: var(--radius-sm);
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s;
  text-decoration: none;
}
.btn-primary:hover { transform: translateY(-1px); box-shadow: 0 4px 16px rgba(0,212,255,0.3); }

.btn-outline {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 10px 24px;
  background: transparent;
  color: var(--text-secondary);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
  text-decoration: none;
}
.btn-outline:hover { border-color: var(--accent-blue); color: var(--accent-blue); }

.filter-chips { display: flex; gap: 6px; flex-wrap: wrap; }
.chip {
  padding: 6px 14px;
  background: var(--bg-card);
  border: 1px solid var(--border);
  color: var(--text-secondary);
  border-radius: 20px;
  font-size: 12px;
  cursor: pointer;
  transition: all 0.2s;
}
.chip.active, .chip:hover { background: rgba(0,212,255,0.1); color: var(--accent-blue); border-color: rgba(0,212,255,0.3); }

.full-feed-controls {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-bottom: 16px;
  flex-wrap: wrap;
}

/* ===== 弹窗 ===== */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.7);
  backdrop-filter: blur(4px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 200;
}
.modal-overlay.hidden { display: none; }

.modal {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 28px;
  max-width: 580px;
  width: 90%;
  max-height: 85vh;
  overflow-y: auto;
  position: relative;
  animation: modalIn 0.3s ease-out;
}
@keyframes modalIn {
  from { opacity: 0; transform: scale(0.95) translateY(10px); }
  to { opacity: 1; transform: scale(1) translateY(0); }
}

.modal-close {
  position: absolute;
  top: 16px; right: 16px;
  background: none;
  border: none;
  color: var(--text-muted);
  font-size: 24px;
  cursor: pointer;
  width: 32px; height: 32px;
  display: flex; align-items: center; justify-content: center;
  border-radius: 50%;
  transition: all 0.2s;
}
.modal-close:hover { background: rgba(255,255,255,0.1); color: var(--text-primary); }

.modal-header { display: flex; align-items: center; gap: 14px; margin-bottom: 20px; }
.modal-token-icon {
  width: 56px; height: 56px;
  border-radius: 50%;
  background: var(--bg-secondary);
  display: flex; align-items: center; justify-content: center;
  font-size: 28px;
}
.modal-ticker { color: var(--accent-blue); font-weight: 600; margin-right: 8px; }
.modal-chain {
  font-size: 11px;
  padding: 3px 8px;
  background: rgba(123,47,247,0.1);
  color: var(--accent-purple);
  border-radius: 4px;
}

.modal-stats {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
  margin-bottom: 20px;
}
.modal-stat {
  background: var(--bg-secondary);
  padding: 12px;
  border-radius: var(--radius-sm);
  display: flex;
  flex-direction: column;
}
.modal-stat-label { font-size: 11px; color: var(--text-muted); }
.modal-stat-value { font-size: 18px; font-weight: 700; margin-top: 2px; }

.modal-chart-container { margin-bottom: 20px; }
.modal-chart-container h3 { font-size: 14px; margin-bottom: 10px; }
.modal-chart-container canvas { width: 100%; height: 150px; }

.modal-tweets { margin-bottom: 20px; }
.modal-tweets h3 { font-size: 14px; margin-bottom: 10px; }

.modal-actions { display: flex; gap: 10px; flex-wrap: wrap; }

/* ===== Toast ===== */
.toast-container {
  position: fixed;
  top: 80px; right: 24px;
  z-index: 300;
  display: flex;
  flex-direction: column;
  gap: 8px;
  pointer-events: none;
}

.toast {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  padding: 14px 18px;
  display: flex;
  align-items: center;
  gap: 10px;
  box-shadow: var(--shadow-lg);
  animation: toastIn 0.4s ease-out;
  pointer-events: auto;
  max-width: 380px;
  backdrop-filter: blur(20px);
}
.toast.success { border-left: 3px solid var(--accent-green); }
.toast.warning { border-left: 3px solid var(--accent-yellow); }
.toast.alert { border-left: 3px solid var(--accent-red); }
.toast.info { border-left: 3px solid var(--accent-blue); }

@keyframes toastIn {
  from { opacity: 0; transform: translateX(40px); }
  to { opacity: 1; transform: translateX(0); }
}
@keyframes toastOut {
  from { opacity: 1; transform: translateX(0); }
  to { opacity: 0; transform: translateX(40px); }
}

.toast-icon { font-size: 20px; flex-shrink: 0; }
.toast-content { flex: 1; }
.toast-title { font-weight: 600; font-size: 13px; }
.toast-desc { font-size: 12px; color: var(--text-muted); margin-top: 2px; }

/* ===== 响应式 ===== */
@media (max-width: 1024px) {
  .dashboard-grid { grid-template-columns: 1fr; }
}
@media (max-width: 768px) {
  .sidebar { display: none; }
  .main-content { margin-left: 0; }
  .topbar-center { display: none; }
  .stats-row { grid-template-columns: repeat(2, 1fr); }
  .modal { padding: 20px; }
}
</style>